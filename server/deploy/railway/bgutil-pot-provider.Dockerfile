# Official image published to Docker Hub by the upstream project
# (https://github.com/Brainicism/bgutil-ytdlp-pot-provider). Note the
# repo/image name is "bgutil-ytdlp-pot-provider" (no dash between "yt"
# and "dlp"). There is no GHCR mirror — only Docker Hub.
FROM docker.io/brainicism/bgutil-ytdlp-pot-provider:latest

EXPOSE 4416